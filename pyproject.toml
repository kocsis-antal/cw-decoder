[project]
name = "cw-morse-layered-pipeline"
version = "0.1.0"
description = "Layered real-time CW/Morse streaming receiver and decoder."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["app/tests"]
pythonpath = ["app/src"]
