[project]
name = "cw-morse-layered-pipeline"
version = "0.1.0"
description = "Experimental CW Morse signal generator and decoder."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["app/tests"]
pythonpath = ["app/src"]
