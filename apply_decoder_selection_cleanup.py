#!/usr/bin/env python3
from pathlib import Path

ROOT = Path(__file__).resolve().parent
REPLACEMENTS = [('app/src/cw/decoder/api.py', '    This is not a score and not a ranking.  The only decoder-owned quality fact\n    exposed here is how many Morse tokens could not be mapped to a valid\n    character.  Invalid tokens are visible in text with the decode error marker;\n    a literal \'?\' remains a valid Morse character.\n    """\n\n    text: str\n    unresolved_tokens: int = 0\n    tokens: tuple[DecodeToken, ...] = ()\n', '    This is not a ranking. ``timing_quality`` is the decoder-owned physical\n    timing-fit value; lower is better. ``unresolved_tokens`` is descriptive\n    output only and must not be used as a hidden decoder rank. Selection owns\n    the comparison between candidates.\n    """\n\n    text: str\n    unresolved_tokens: int = 0\n    tokens: tuple[DecodeToken, ...] = ()\n    timing_quality: float = 0.0\n'), ('app/src/cw/decoder/config.py', '    unit_candidate_spread: float = 0.0\n    unit_candidate_steps: int = 1\n    punctuation_penalty: float = 0.0\n', '    unit_candidate_spread: float = 0.0\n    unit_candidate_steps: int = 1\n'), ('app/src/cw/decoder/config.py', '    if config.unit_candidate_steps < 1:\n        raise ValueError("unit_candidate_steps must be positive")\n    if config.punctuation_penalty < 0:\n        raise ValueError("punctuation_penalty must not be negative")\n', '    if config.unit_candidate_steps < 1:\n        raise ValueError("unit_candidate_steps must be positive")\n'), ('app/src/cw/decoder/run_decoder.py', '                    unresolved_tokens=decoded.unresolved_tokens,\n                    timing_quality=round(float(timing_quality_score(decoded, punctuation_penalty=config.punctuation_penalty)), 6),\n', '                    unresolved_tokens=decoded.unresolved_tokens,\n                    timing_quality=round(float(timing_quality_score(decoded)), 6),\n'), ('app/src/cw/decoder/run_decoder.py', 'def _to_public_answers(answers: list[_DecodedPath]) -> list[DecodedText]:\n    return [DecodedText(text=answer.text, unresolved_tokens=answer.unresolved_tokens, tokens=answer.tokens) for answer in answers]\n', 'def _to_public_answers(answers: list[_DecodedPath]) -> list[DecodedText]:\n    return [\n        DecodedText(\n            text=answer.text,\n            unresolved_tokens=answer.unresolved_tokens,\n            tokens=answer.tokens,\n            timing_quality=answer.timing_quality,\n        )\n        for answer in answers\n    ]\n'), ('app/src/cw/decoder/run_decoder.py', 'def _answer_sort_key(answer: _DecodedPath) -> tuple[int, float, float]:\n    return (\n        answer.unresolved_tokens,\n        answer.timing_quality,\n    )\n', "def _answer_sort_key(answer: _DecodedPath) -> tuple[float]:\n    # Decoder-local ordering is timing-only. Selection must never infer quality\n    # from the answer's list position/rank.\n    return (answer.timing_quality,)\n"), ('app/src/cw/decoder/timing.py', 'def timing_quality_score(decoded: TimedDecode, *, punctuation_penalty: float = 0.0) -> float:\n    tones = [run for run in decoded.classified_runs if run.kind is HardRunKind.TONE]\n    gaps = [run for run in decoded.classified_runs if run.kind is HardRunKind.GAP]\n    dots = [run for run in tones if run.symbol == "."]\n    dashes = [run for run in tones if run.symbol == "-"]\n    unresolved_tokens = decoded.unresolved_tokens\n    token_count = len([token for token in decoded.tokens if token != "/"])\n    punctuation_count = sum(1 for char in decoded.text if char and not char.isspace() and not char.isalnum())\n\n    return (\n        _unresolved_token_penalty(unresolved_tokens, token_count)\n        + _invalid_token_penalty(decoded.tokens)\n        + _too_few_tokens_penalty(token_count)\n        + _tone_ratio_error(dots, dashes) * 120.0\n        + _gap_min_error(gaps) * 80.0\n        + _unit_cv(dots) * 60.0\n        + punctuation_count * punctuation_penalty\n    )\n', 'def timing_quality_score(decoded: TimedDecode) -> float:\n    """Measure only the physical Morse timing fit. Lower is better.\n\n    Unresolved-token count, text length and character content deliberately do\n    not belong here. Those are visible to selection independently.\n    """\n\n    tones = [run for run in decoded.classified_runs if run.kind is HardRunKind.TONE]\n    gaps = [run for run in decoded.classified_runs if run.kind is HardRunKind.GAP]\n    dots = [run for run in tones if run.symbol == "."]\n    dashes = [run for run in tones if run.symbol == "-"]\n\n    return (\n        _tone_ratio_error(dots, dashes) * 120.0\n        + _gap_min_error(gaps) * 80.0\n        + _unit_cv(dots) * 60.0\n    )\n'), ('app/src/cw/decoder/timing.py', 'def _unresolved_token_penalty(unresolved_count: int, token_count: int) -> float:\n    if unresolved_count <= 0:\n        return 0.0\n    return 240.0 * unresolved_count / max(token_count, 1)\n\n\n', ''), ('app/src/cw/decoder/timing.py', 'def _invalid_token_penalty(tokens: tuple[str, ...]) -> float:\n    return sum(80.0 for token in tokens if token and token != "/" and len(token) > 6)\n\n\ndef _too_few_tokens_penalty(token_count: int) -> float:\n    if token_count == 0:\n        return 1000.0\n    if token_count == 1:\n        return 100.0\n    return 0.0\n\n\n', ''), ('app/src/cw/selection/debug.py', 'class SelectionPathDebug:\n    analyzer: str\n    decoder: str\n    unresolved_tokens: int\n', 'class SelectionPathDebug:\n    analyzer: str\n    decoder: str\n    unresolved_tokens: int = 0\n    timing_quality: float = 0.0\n'), ('app/src/cw/selection/debug.py', '    neighbor_stability: int\n    support_score: float = 0.0\n', '    neighbor_stability: int\n    timing_quality: float = 0.0\n    support_score: float = 0.0\n'), ('app/src/cw/selection/arbiter.py', 'from cw.decoder.tokens import DecodeToken, token_signature\n', 'from cw.decoder.tokens import DecodeToken, TOKEN_UNKNOWN, token_signature\nfrom cw.morse_table import DECODE_ERROR_MARKER\n'), ('app/src/cw/selection/arbiter.py', '    unresolved_tokens: int\n    analyzer: str\n    decoder: str\n    encounter_order: int\n    unknown_ratio: float = 0.0\n    answer_rank: int = 0\n', '    timing_quality: float\n    analyzer: str\n    decoder: str\n    encounter_order: int\n    unknown_ratio: float = 0.0\n'), ('app/src/cw/selection/arbiter.py', '    support_score: float\n    unknown_penalty_score: float\n', '    support_score: float\n    timing_quality: float\n    unknown_penalty_score: float\n'), ('app/src/cw/selection/arbiter.py', '    def ranking_tuple(self) -> tuple[float, int, int, int]:\n        """Higher is better, except unresolved tokens which is negated.\n\n        With a single activity model in the normal runtime path, selection\n        rewards agreement between its probability variants directly.\n        """\n\n        return (\n            self.final_score,\n            -self.unresolved_tokens,\n            self.neighbor_stability,\n            self.support_count,\n        )\n', '    def ranking_tuple(self) -> tuple[float, float, int, int]:\n        """Rank independent facts once.\n\n        ``final_score`` already contains the selection-owned UNKNOWN penalty.\n        Timing quality is a separate decoder-owned physical tie-break.\n        """\n\n        return (\n            self.final_score,\n            -self.timing_quality,\n            self.neighbor_stability,\n            self.support_count,\n        )\n'), ('app/src/cw/selection/arbiter.py', '        for result in track.results:\n            for answer_rank, answer in enumerate(result.answers):\n', '        for result in track.results:\n            for answer in result.answers:\n'), ('app/src/cw/selection/arbiter.py', '                        unresolved_tokens=max(0, int(answer.unresolved_tokens)),\n                        analyzer=track.analyzer,\n                        decoder=result.decoder,\n                        encounter_order=encounter_order,\n                        unknown_ratio=max(0.0, float(track.unknown_ratio)),\n                        answer_rank=answer_rank,\n', '                        timing_quality=_normalized_timing_quality(answer.timing_quality),\n                        analyzer=track.analyzer,\n                        decoder=result.decoder,\n                        encounter_order=encounter_order,\n                        unknown_ratio=max(0.0, float(track.unknown_ratio)),\n'), ('app/src/cw/selection/arbiter.py', '    paths = tuple(group.paths)\n    best_paths = _best_paths_by_unresolved(paths)\n    min_unresolved = min(path.unresolved_tokens for path in best_paths)\n    support_count = len({path.analyzer for path in best_paths})\n    support_score = sum(_answer_rank_weight(path.answer_rank) for path in best_paths)\n    unknown_penalty_score = _unknown_penalty(min_unresolved)\n    final_score = support_score - unknown_penalty_score\n    neighbor_stability = _neighbor_stability(list(best_paths), analyzer_infos)\n    score = _GroupScore(\n        unresolved_tokens=min_unresolved,\n        support_count=support_count,\n        support_score=support_score,\n        unknown_penalty_score=unknown_penalty_score,\n', '    best_paths = _best_paths_by_analyzer(tuple(group.paths))\n    unresolved_tokens = _unresolved_token_count(group.tokens, group.text)\n    support_count = len(best_paths)\n    support_score = float(support_count)\n    timing_quality = _mean_timing_quality(best_paths)\n    unknown_penalty_score = _unknown_penalty(unresolved_tokens)\n    final_score = support_score - unknown_penalty_score\n    neighbor_stability = _neighbor_stability(list(best_paths), analyzer_infos)\n    score = _GroupScore(\n        unresolved_tokens=unresolved_tokens,\n        support_count=support_count,\n        support_score=support_score,\n        timing_quality=timing_quality,\n        unknown_penalty_score=unknown_penalty_score,\n'), ('app/src/cw/selection/arbiter.py', 'def _best_paths_by_unresolved(paths: tuple[_DecodedPath, ...]) -> tuple[_DecodedPath, ...]:\n    min_unresolved = min(path.unresolved_tokens for path in paths)\n    return tuple(path for path in paths if path.unresolved_tokens == min_unresolved)\n\n\ndef _answer_rank_weight(answer_rank: int) -> float:\n    # Decoder answers are already sorted by timing quality.  A lower-ranked\n    # alternative is useful as weak support, but it must not beat a top answer\n    # merely because it appears as a fallback under several probability variants.\n    return 1.0 / float(max(1, int(answer_rank) + 1))\n\n\n', 'def _best_paths_by_analyzer(paths: tuple[_DecodedPath, ...]) -> tuple[_DecodedPath, ...]:\n    best: dict[str, _DecodedPath] = {}\n    for path in paths:\n        current = best.get(path.analyzer)\n        if current is None or (path.timing_quality, path.encounter_order) < (\n            current.timing_quality,\n            current.encounter_order,\n        ):\n            best[path.analyzer] = path\n    return tuple(best.values())\n\n\ndef _unresolved_token_count(tokens: tuple[DecodeToken, ...], text: str) -> int:\n    # Tokens are the source of truth in the runtime. The text fallback keeps\n    # hand-built/test candidates honest without trusting decoder-owned ranking data.\n    token_count = sum(1 for token in tokens if token.kind == TOKEN_UNKNOWN)\n    return max(token_count, text.count(DECODE_ERROR_MARKER))\n\n\ndef _normalized_timing_quality(value: float) -> float:\n    quality = float(value)\n    return quality if isfinite(quality) and quality >= 0.0 else float("inf")\n\n\ndef _mean_timing_quality(paths: tuple[_DecodedPath, ...]) -> float:\n    if not paths:\n        return float("inf")\n    return sum(path.timing_quality for path in paths) / len(paths)\n\n\n'), ('app/src/cw/selection/arbiter.py', '            support_count=group.score.support_count,\n            support_score=group.score.support_score,\n', '            support_count=group.score.support_count,\n            timing_quality=group.score.timing_quality,\n            support_score=group.score.support_score,\n'), ('app/src/cw/selection/arbiter.py', '                    analyzer=path.analyzer,\n                    decoder=path.decoder,\n                    unresolved_tokens=path.unresolved_tokens,\n', '                    analyzer=path.analyzer,\n                    decoder=path.decoder,\n                    unresolved_tokens=group.score.unresolved_tokens,\n                    timing_quality=path.timing_quality,\n'), ('app/src/cw/app/debug_output.py', 'class DebugDecodedText:\n    text: str\n    unresolved_tokens: int\n    tokens: tuple[DecodeToken, ...] = ()\n', 'class DebugDecodedText:\n    text: str\n    unresolved_tokens: int\n    tokens: tuple[DecodeToken, ...] = ()\n    timing_quality: float = 0.0\n'), ('app/src/cw/app/debug_output.py', '{"text": answer.text, "tokens": tokens_to_dicts(answer.tokens), "unresolved_tokens": answer.unresolved_tokens}\n', '{"text": answer.text, "tokens": tokens_to_dicts(answer.tokens), "unresolved_tokens": answer.unresolved_tokens, "timing_quality": _rounded_float(answer.timing_quality)}\n'), ('app/src/cw/app/debug_output.py', '                    "support_count": group.support_count,\n                    "support_score": _rounded_float(group.support_score),\n', '                    "support_count": group.support_count,\n                    "timing_quality": _rounded_float(group.timing_quality),\n                    "support_score": _rounded_float(group.support_score),\n'), ('app/src/cw/app/debug_output.py', '                            "decoder": path.decoder,\n                            "unresolved_tokens": path.unresolved_tokens,\n', '                            "decoder": path.decoder,\n                            "unresolved_tokens": path.unresolved_tokens,\n                            "timing_quality": _rounded_float(path.timing_quality),\n'), ('app/src/cw/app/debug_output.py', 'DebugDecodedText(text=answer.text, tokens=answer.tokens, unresolved_tokens=max(0, int(answer.unresolved_tokens)))\n', 'DebugDecodedText(text=answer.text, tokens=answer.tokens, unresolved_tokens=max(0, int(answer.unresolved_tokens)), timing_quality=max(0.0, float(answer.timing_quality)))\n'), ('app/src/cw/ui/debug_view.py', '                f"support={int(group.get(\'support_count\') or 0)} "\n                f"neighbors={int(group.get(\'neighbor_stability\') or 0)} "\n', '                f"support={int(group.get(\'support_count\') or 0)} "\n                f"timing={float(group.get(\'timing_quality\') or 0.0):.2f} "\n                f"neighbors={int(group.get(\'neighbor_stability\') or 0)} "\n'), ('app/src/cw/ui/debug_view.py', 'f"{_quote(str(answer.get(\'text\') or \'\'))}/bad={int(answer.get(\'unresolved_tokens\') or 0)}"\n', 'f"{_quote(str(answer.get(\'text\') or \'\'))}/bad={int(answer.get(\'unresolved_tokens\') or 0)}/timing={float(answer.get(\'timing_quality\') or 0.0):.2f}"\n'), ('README.md', 'Az `UNKNOWN` futam nem automatikusan SPACE és nem is automatikusan MARK: a dekóder helyileg vizsgálja a lehetséges értelmezéseket.\n', 'Egy **futam** egy megszakítás nélküli, azonos állapotú időszakasz (például 80 ms MARK vagy 40 ms UNKNOWN).\n\nMinden egyes `UNKNOWN` futam **külön-külön ágazik** MARK és SPACE irányba. Ez nem globális „minden UNKNOWN legyen MARK” / „minden UNKNOWN legyen SPACE” próbálkozás: N darab UNKNOWN legfeljebb 2^N konkrét értelmezési ágat ad. Az UNKNOWN időtartama nem változik; az adott ágban csak MARK-ként vagy SPACE-ként értelmezzük.\n\nMinden konkrét UNKNOWN→MARK/SPACE ághoz a decoder újra megbecsüli az adott futamsorhoz illő Morse alapidőt (`unit_s`). A decoder a jelöltekhez tisztán időzítés-alapú `timing_quality` értéket ad; a jelöltek közötti tényleges rangsorolás a selection feladata.\n'), ('README.md', '- hány signal-paraméterváltozat támogatja ugyanazt az eredményt;\n- hány feloldatlan token maradt;\n- a dekóder alternatíváin belüli rang;\n- szomszédos signal-paraméterek stabilitása.\n', '- hány signal-paraméterváltozat támogatja ugyanazt az eredményt;\n- hány `unknown` token maradt — ezt a selection maga állapítja meg a tokenekből;\n- a decoder által mért, tisztán időzítés-alapú `timing_quality`;\n- szomszédos signal-paraméterek stabilitása.\n\nA decoder válaszainak listapozíciója nem selection-bemenet: nincs `answer_rank` alapú részpontszám.\n'), ('app/tests/test_decoder_layer.py', '    assert answer_fields == {"text", "unresolved_tokens", "tokens"}\n', '    assert answer_fields == {"text", "unresolved_tokens", "tokens", "timing_quality"}\n'), ('app/tests/test_decoder_layer.py', '    assert "answer.text" not in source\n', '    assert "answer.text" not in source\n    assert "answer.unresolved_tokens" not in source\n    assert "answer.timing_quality" in source\n')]

changed = []
for rel, old, new in REPLACEMENTS:
    path = ROOT / rel
    text = path.read_text(encoding="utf-8")
    if old not in text:
        raise SystemExit(
            f"Expected block not found in {rel}. No blind patching performed for this block."
        )
    path.write_text(text.replace(old, new, 1), encoding="utf-8")
    changed.append(rel)
    print("updated", rel)

print("\nDone. Run:")
print("docker compose -f infra/compose.yml run --rm -T cw pytest -q")
